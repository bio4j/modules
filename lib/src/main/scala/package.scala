// package ohnosequences.bio4j

// import shapeless._
// import ohnosequences.typesets._

// package object statika {

//   // TODO: move it to type-sets:

//   type UnionOf[A <: TypeSet, B <: TypeSet] = { type is[O <: TypeSet] = UnionSets[A, B] { type Out = O } }

//   type nonEmpty[S <: TypeSet] = S#Bound#get =:!= ∅.Bound#get

// }
